appId: com.stoicpiggy.app
tags:
  - auth
---
# Screen: Wins (stats + badges). Reached from the tab bar.
- runFlow: ../subflows/sign-in.yaml
- tapOn:
    id: "tab-wins"
- extendedWaitUntil:
    visible:
      id: "wins-screen"
    timeout: 30000
