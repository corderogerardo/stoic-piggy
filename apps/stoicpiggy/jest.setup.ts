// Global test setup for the mobile app.
//
// Our default tests render plain RN components, so no native mocks are required.
// When you start testing animated components, enable the Reanimated mock:
//
//   jest.mock('react-native-reanimated', () => require('react-native-reanimated/mock'));
export {};
