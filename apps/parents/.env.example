# Parents dashboard — environment variables

# Where the NestJS tRPC API is mounted (REST lives at /api, tRPC at /trpc).
NEXT_PUBLIC_API_URL=http://localhost:3001/trpc

# Parents now sign in, so the dashboard shows the authenticated parent's own
# children — no demo parent id needed. Seeded demo login: patricia@stoicpiggy.dev / piggy1234
